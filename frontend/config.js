// SentinelAI - إعدادات الواجهة
//
// عنوان خادم الـ FastAPI Backend. غيّر هذه القيمة فقط إذا شغّلت الخادم على
// عنوان أو منفذ مختلف (مثال: uvicorn api.main:app --port 9000).
const CONFIG = {
  API_BASE_URL: "http://127.0.0.1:8000",
};
